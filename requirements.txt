altgraph==0.17.4
importlib_metadata==8.5.0
numpy==1.24.4
packaging==24.2
pefile==2023.2.7
pillow==10.3.0
pyinstaller==6.12.0
pyinstaller-hooks-contrib==2025.1
pywin32-ctypes==0.2.3
zipp==3.20.2
requests==2.32.4
networkx>=3.0

# Optional: ISA-L drop-in replacement for zlib. When installed, nbt.py uses it
# to decompress region chunks ~2x faster than stdlib zlib. Safe to omit; the
# code falls back to zlib transparently.
isal>=1.8.0; platform_machine == "x86_64" or platform_machine == "AMD64"